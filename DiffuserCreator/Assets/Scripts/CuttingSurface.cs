using UnityEngine;

namespace DiffuserCreator
{
    // Marker component. Blocks in Cutting mode raycast against this object's collider on the
    // cutting layer; the collider geometry is the relief shape carved into the diffuser.
    public class CuttingSurface : MonoBehaviour
    {
    }
}
